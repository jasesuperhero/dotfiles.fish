extends: default
rules:
  document-start: disable
  comments:
    min-spaces-from-content: 1
    require-starting-space: false
  # GitHub Actions requires the top-level `on:` key, which yamllint's truthy
  # rule would otherwise flag. Still check truthy *values*.
  truthy:
    check-keys: false
