*~
dist
.claude
.DS_Store

# Alfred preferences: huge (emoji-pack snippets), machine-specific, and Alfred
# has its own sync — keep the bundle out of the repo.
alfred/

# Karabiner auto-generated dated backups (pure churn). Karabiner-Elements writes
# these into its config dir, which is symlinked to karabiner/config/.
karabiner/config/automatic_backups/
